{
  "name": "facebook-mass-v2",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^15.6.1",
    "react-dom": "^15.6.1"
  },
  "devDependencies": {
    "ftp-deploy": "^1.2.2",
    "react-scripts": "1.0.10"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "PUBLIC_URL=. yarn build && node deploy.js",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
